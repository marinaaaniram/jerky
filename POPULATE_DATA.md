# 📊 Заполнение БД тестовыми данными (1000+ записей)

Быстрый старт для симуляции работы системы с реальными данными.

## ⚡ Самый быстрый способ (30 секунд)

```bash
# 1. Убедитесь, что Docker запущен
docker-compose ps

# 2. Если не запущен:
docker-compose up --build -d
sleep 20

# 3. Запустите скрипт с интерактивным меню
chmod +x scripts/populate.sh
bash scripts/populate.sh

# 4. Выберите опцию 1 (⚡ БЫСТРЫЙ способ)
```

**Готово за 30 секунд!** 🚀

## 📋 Варианты заполнения

### Вариант 1: ⚡ Быстрый (30 сек) - РЕКОМЕНДУЕТСЯ

```bash
chmod +x scripts/fast-populate-db.sh
bash scripts/fast-populate-db.sh 1000
```

**Результат:**
- Напрямую в PostgreSQL (минует API)
- ~1000 записей на таблицу
- **Время: ~30 секунд**

### Вариант 2: 🚀 Обычный через API (20-25 мин)

```bash
chmod +x scripts/populate-db.sh
bash scripts/populate-db.sh http://localhost:3000
```

**Результат:**
- Все через API endpoints
- Протестирует все endpoints
- **Время: ~20-25 минут**
- Полезно для тестирования API

### Вариант 3: 🎯 Интерактивное меню

```bash
chmod +x scripts/populate.sh
bash scripts/populate.sh
# Выберите способ в меню
```

## ✅ Проверка результата

### В веб-приложении
```
1. Откройте http://localhost:5173
2. Входите: ivan@jerky.com / password123
3. Перейдите на Dashboard - видите большие числа (1000+)
4. Откройте любой раздел (Заказы, Клиенты, Товары) - полно данных
```

### В БД напрямую
```bash
docker exec -it jerky-postgres psql -U jerky_user -d jerky << EOF
SELECT COUNT(*) as users FROM users;
SELECT COUNT(*) as customers FROM customers;
SELECT COUNT(*) as products FROM products;
SELECT COUNT(*) as orders FROM orders;
\q
EOF
```

## 📊 Что создается?

| Таблица | Количество | Назначение |
|---------|-----------|-----------|
| Users | 1000 | Пользователи системы |
| Customers | 1000 | Клиенты для доставки |
| Products | 1000 | Товары в каталоге |
| Orders | 1000 | Заказы доставки |
| OrderItems | 3000-5000 | Товары в заказах |
| Payments | 1000 | Платежи от клиентов |
| PriceRules | 1000 | Специальные цены |

**Итого: ~7000+ записей**

## 🎯 Когда использовать какой вариант

### Используйте ⚡ Быстрый (SQL):
- ✅ Нужны данные как можно быстрее
- ✅ Тестируете UI/UX
- ✅ Проверяете производительность
- ✅ Нужно заполнить большой объем

### Используйте 🚀 Обычный (API):
- ✅ Тестируете API endpoints
- ✅ Проверяете валидацию
- ✅ Хотите видеть логи запросов
- ✅ Меньше потребление ресурсов

## 🔧 Кастомизация

### Изменить количество записей
```bash
# Быстрый способ - 5000 записей на таблицу
bash scripts/fast-populate-db.sh 5000

# Обычный способ - автоматически выберет 1000
# (изменить в скрипте RECORDS_PER_TABLE)
```

### Изменить параметры БД
```bash
# Для fast-populate-db.sh
DB_HOST=192.168.1.100 \
DB_PORT=5432 \
DB_NAME=jerky \
DB_USER=jerky_user \
DB_PASSWORD=jerky_password \
bash scripts/fast-populate-db.sh 1000
```

### Изменить API URL
```bash
# Для populate-db.sh
bash scripts/populate-db.sh http://production-api.com:3000
```

## 🐛 Проблемы и решения

### ❌ "PostgreSQL контейнер не запущен"
```bash
docker-compose up -d postgres backend
sleep 20
bash scripts/fast-populate-db.sh 1000
```

### ❌ "psql: command not found"
```bash
# Установите psql
# macOS
brew install libpq

# Ubuntu/Debian
sudo apt-get install postgresql-client

# Или используйте через Docker
docker exec -it jerky-postgres psql ...
```

### ❌ "API недоступна"
```bash
# Проверьте backend
docker logs jerky-backend -f

# Если не запущен
docker-compose up -d backend
sleep 30
bash scripts/populate-db.sh
```

### ❌ "Не удалось получить токен"
```bash
# Проверьте, что seeding выполнен
docker logs jerky-backend | grep seed

# Если нет, запустите вручную
docker exec jerky-backend npm run seed:run
```

## 📈 Производительность

### Что проверить с заполненной БД?

1. **Скорость загрузки Dashboard**
   - Должен загруститься за < 2 сек

2. **Поиск (Global Search)**
   - Поиск по 1000+ записей должен быть быстрым

3. **Фильтры и сортировка**
   - Таблицы должны быстро фильтроваться

4. **Пагинация**
   - Работает ли корректно с большим числом страниц?

5. **Аналитика**
   - Графики считаются правильно?

## 🔄 Очистка и перезагрузка

### Удалить все данные и начать заново

```bash
# Способ 1: Полная очистка
docker-compose down -v
docker-compose up -d
bash scripts/fast-populate-db.sh 1000

# Способ 2: Только БД
docker exec jerky-postgres psql -U jerky_user -d jerky << EOF
TRUNCATE TABLE order_items, orders, payments, price_rules, products, customers, users CASCADE;
\q
EOF

bash scripts/fast-populate-db.sh 1000
```

## 📚 Дополнительная справка

```bash
# Подробная справка по populate-db.sh (API)
bash scripts/populate-db.sh --help
# или
cat scripts/POPULATE_README.md

# Интерактивное меню со справкой
bash scripts/populate.sh
# выберите опцию 3
```

## 🚀 Дальнейшие шаги

Когда база заполнена, вы можете:

1. **Тестировать функциональность**
   - Создавать/редактировать заказы
   - Добавлять платежи
   - Менять статусы

2. **Проверить производительность**
   - Загружаются ли 1000 записей быстро?
   - Работает ли поиск?

3. **Проверить аналитику**
   - Правильно ли считаются метрики?
   - Строятся ли графики?

4. **Нагрузочное тестирование**
   - Используйте инструменты типа Artillery или LoadImpact
   - Проверьте как система работает под нагрузкой

---

**Выберите способ и начните тестировать! 🎉**

```bash
# Самый быстрый старт
docker-compose up -d && sleep 20 && bash scripts/fast-populate-db.sh 1000 && echo "✅ Готово! Откройте http://localhost:5173"
```
